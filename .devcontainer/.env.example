# Copy this file to .devcontainer/.env before opening the container.

# Required: absolute host path to the data root containing external/, interim/,
# and processed/. The directory is mounted writable at /home/dcuser/data.
SEIS_INTERP_DATA_ROOT=

# Optional proxy settings.
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=

# Optional non-interactive authentication. Interactive sign-in also works.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GH_TOKEN=
GITHUB_TOKEN=

# Use "latest" for a rolling developer environment or pin exact versions.
CODEX_VERSION=latest
CLAUDE_CODE_VERSION=latest
